<UserControl x:Class="RoeSnip.Overlay.ToolbarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Focusable="False"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True">
    <UserControl.Resources>
        <Style x:Key="ToolButtonStyle" TargetType="ToggleButton">
            <Setter Property="MinWidth" Value="36"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Background" Value="#2A2A2E"/>
            <Setter Property="Foreground" Value="#EAEAEA"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#3A7CFD"/>
                                <Setter Property="BorderBrush" Value="#5C9CFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#6A6A70"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="Margin" Value="3,2"/>
            <Setter Property="Background" Value="#2A2A2E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ColorSwatchStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="BorderBrush" Value="White"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StrokeWidthSwatchStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Background" Value="#2A2A2E"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <Ellipse Fill="White" Width="{TemplateBinding Tag}" Height="{TemplateBinding Tag}"
                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#3A7CFD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="#EE1B1B1D" CornerRadius="6" BorderBrush="#3F3F46" BorderThickness="1">
        <StackPanel Orientation="Horizontal" Margin="4">
            <ToggleButton x:Name="SelectToolButton" Style="{StaticResource ToolButtonStyle}" Content="Sel" ToolTip="Select / move (default)" IsChecked="True" Click="OnSelectToolClick"/>
            <ToggleButton x:Name="RectToolButton" Style="{StaticResource ToolButtonStyle}" Content="Rect" ToolTip="Rectangle" Click="OnRectToolClick"/>
            <ToggleButton x:Name="EllipseToolButton" Style="{StaticResource ToolButtonStyle}" Content="Elli" ToolTip="Ellipse" Click="OnEllipseToolClick"/>
            <ToggleButton x:Name="ArrowToolButton" Style="{StaticResource ToolButtonStyle}" Content="Arrow" ToolTip="Arrow" Click="OnArrowToolClick"/>
            <ToggleButton x:Name="LineToolButton" Style="{StaticResource ToolButtonStyle}" Content="Line" ToolTip="Line" Click="OnLineToolClick"/>
            <ToggleButton x:Name="FreehandToolButton" Style="{StaticResource ToolButtonStyle}" Content="Free" ToolTip="Freehand" Click="OnFreehandToolClick"/>
            <ToggleButton x:Name="TextToolButton" Style="{StaticResource ToolButtonStyle}" Content="Text" ToolTip="Text" Click="OnTextToolClick"/>

            <Border Width="1" Margin="4,3" Background="#3F3F46"/>

            <StackPanel x:Name="ColorSwatchPanel" Orientation="Horizontal" VerticalAlignment="Center"/>

            <Border Width="1" Margin="4,3" Background="#3F3F46"/>

            <StackPanel x:Name="StrokeWidthPanel" Orientation="Horizontal" VerticalAlignment="Center"/>

            <Border Width="1" Margin="4,3" Background="#3F3F46"/>

            <Button x:Name="UndoButton" Style="{StaticResource ActionButtonStyle}" Content="Undo" ToolTip="Undo (Ctrl+Z)" Click="OnUndoClick"/>

            <Border Width="1" Margin="4,3" Background="#3F3F46"/>

            <Button x:Name="CopyButton" Style="{StaticResource ActionButtonStyle}" Content="Copy" ToolTip="Copy (Ctrl+C)" Background="#2E7D32" Click="OnCopyClick"/>
            <Button x:Name="SaveButton" Style="{StaticResource ActionButtonStyle}" Content="Save" ToolTip="Save (Ctrl+S)" Background="#1565C0" Click="OnSaveClick"/>
            <Button x:Name="SaveHdrButton" Style="{StaticResource ActionButtonStyle}" Content="Save HDR" ToolTip="Save the untouched HDR original" Background="#6A1B9A" Click="OnSaveHdrClick"/>
        </StackPanel>
    </Border>
</UserControl>
