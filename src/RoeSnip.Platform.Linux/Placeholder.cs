namespace RoeSnip.Platform.Linux;

/// <summary>WP-X1 stub so the project (and the solution) compiles before WP-X4 lands. WP-X4 owns
/// everything in this project (PLAN-XPLAT.md §3.4) and should DELETE this file when it adds the
/// real LinuxCaptureBackend / PortalScreenshotCapturer / X11Capturer files.</summary>
internal static class Placeholder
{
}
